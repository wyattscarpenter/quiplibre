{
  "name": "quiplibre",
  "version": "2.3.0-dev",
  "description": "A party game about answering prompts cleverly.",
  "main": "quiplibre.html",
  "remark on check script": "I don't expect this check to actually pass, it's just another analysis of the code which is sometimes useful. Also, I discard its error code.",
  "scripts": {
    "check": "node -e \"const fs=require('fs'),{execSync}=require('child_process');const m=fs.readFileSync('quiplibre.html','utf8').match(/<script>\\s*([\\s\\S]*?)\\s*<\\/script>/);if(!m)process.exit(0);fs.writeFileSync('.__tmp.ts',m[1]);try{execSync('tsc --noEmit .__tmp.ts',{stdio:'inherit'})}catch(e){}fs.unlinkSync('.__tmp.ts')\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wyattscarpenter/quiplibre.git"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/wyattscarpenter/quiplibre/issues"
  },
  "homepage": "https://github.com/wyattscarpenter/quiplibre#readme",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.1",
    "http-server": "^14.1.1",
    "typescript": "^5.9.3"
  }
}
